@page
@using ResumeMetadataUI;
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

@{
    ViewData["Title"] = "Metadata Insertion Tool";
}

<div class="alert alert-info">
    <strong>Disclaimer:  We do not store any CVs on our servers. This app is open-source, allowing everyone to view and inspect its code.</strong>
</div>

<div class="container mt-5">
    @if (Model.MessageStatus == MessageType.Warning)
    {
        <div class="alert alert-warning">
            <strong>Warning!</strong> @Html.Raw(Model.OutputText)
        </div>
    }
    else if (Model.MessageStatus == MessageType.Information)
    {
        <div class="alert alert-info">
            <strong>Information:</strong> @Html.Raw(Model.OutputText)
        </div>
    }

    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Metadata Insertion Tool</h4>
            <p>Use this tool to insert relevant search keywords into a Résumé or Curriculum Vitae, in the form of non-intrusive Metadata. As an example, let's say you wish to apply for a job as "Trainer", in your Resume you probably want to add similar variations "Instructor, Coach, Mentor" with variations in a second language. Repeat this process for any skills you possess that have relevance to the job you are applying for.</p>

            <form method="post" enctype="multipart/form-data">
                <div class="form-group">
                    <label for="docxFile">Choose a .docx file:</label>
                    <div class="input-group">
                        <input type="file" asp-for="DocxFile"  class="form-control" accept=".docx" id="sourceDocument" placeholder="Choose file...">
                    </div>
                </div>

                <div class="form-group">
                    <label for="metadata">Metadata to Insert</label>
                    <textarea asp-for="MetadataContent" class="form-control" id="metadata" rows="3" placeholder="Expertise, Skill, Ability, Talent, Proficiency, Capability, Competence, Aptitude, Fähigkeit, Kompetenz, Talent, Befähigung, etc..."></textarea>
                </div>

                <button type="submit" class="btn btn-primary">Generate</button>
            </form>
        </div>
    </div>
</div>
